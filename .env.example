# Divan — Environment Configuration
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to git.

# Path to your OpenClaw workspace directory (the clawd/ folder)
OPENCLAW_WORKSPACE=/path/to/your/clawd

# OpenClaw Gateway WebSocket URL
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18009

# OpenClaw Gateway HTTP URL (used for health checks)
OPENCLAW_GATEWAY_HTTP=http://127.0.0.1:18009

# Your OpenClaw Gateway auth token
# Run `openclaw token` or check ~/.openclaw/config to find yours
OPENCLAW_GATEWAY_TOKEN=your-gateway-token-here
